<header>
</header>

<main>
    <table class="datatable">
        <thead>
        <tr>
            <th>id</th>
            <th>idProduct</th>
            <th>quantity</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($paniers as $pa) : ?>
            <tr>
                <td><?= $this->escapeHtml($pa->_id) ?></td>
                <td><?= $this->escapeHtml($pa->_idProduct) ?></td>
                <td><?= $this->escapeHtml($pa->_quantity) ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</main>